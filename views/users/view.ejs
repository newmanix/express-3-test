<%- include('../partials/header') %>
            <% users.forEach(function(user){ %>
            <div class="panel panel-default" style="margin-top:10%">
              <div class="panel-heading"><h3><%= user.first_name %> <%= user.last_name %></h3></div>
              <div class="panel-body">Something clever like bio type data could go here.  
                We do however have an email: <b><%= user.email %></b></div>
              <div class="panel-footer">Finish with someting clever here as well!</div>
              <% }); %>    
<%- include('../partials/footer') %>
